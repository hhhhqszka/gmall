<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.gmall.user.mapper.UserMapper">

    <select id="selectAllUser" resultType="com.atguigu.gmall.user.bean.UmsMember">
        select t.* from ums_member t
    </select>

    <update id="updateUser" >
        update ums_member SET username='luzerui' WHERE id=1
    </update>

    <!--useGeneratedKeys等是为了将插入的主键返回-->
    <insert id="" useGeneratedKeys="true" keyColumn="id" keyProperty="id"></insert>

</mapper>